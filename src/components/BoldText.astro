---
interface Props {
  text: string;
}

// Render **bold** spans while letting Astro escape all text normally.
const parts = Astro.props.text.split(/\*\*(.+?)\*\*/g);
---

{parts.map((part, index) => index % 2 === 1 ? <strong>{part}</strong> : part)}
